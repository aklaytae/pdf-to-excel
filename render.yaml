services:
  - type: web
    name: pdf-to-excel-statement
    runtime: node
    buildCommand: npm install && pip install --break-system-packages -r requirements.txt
    startCommand: node app.js
    envVars:
      - key: PORT
        value: 10000
    plan: free
