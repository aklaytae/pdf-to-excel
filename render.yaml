services:
  - type: web
    name: pdf-to-excel-statement
    runtime: node
    buildCommand: npm install && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
    startCommand: node app.js
    envVars:
      - key: PORT
        value: 10000
    plan: free
